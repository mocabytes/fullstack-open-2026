sequenceDiagram
    participant browser
    participant server

    Note right of browser: El usuario escribe la nota y hace clic en "Save"

    Note right of browser: El JavaScript de la SPA captura el evento "submit" y evita la recarga de la página
    Note right of browser: El JS añade la nueva nota a la lista localmente y la renderiza en el DOM inmediatamente

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note over server: El servidor recibe el JSON con la nota y la guarda en la base de datos
    server-->>browser: HTTP status code 201 (Created)
    deactivate server

    Note right of browser: El navegador no recarga nada, la nota ya es visible para el usuario